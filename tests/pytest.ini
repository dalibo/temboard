[pytest]
addopts = -v --strict-markers -p no:anyio -p no:cov -p no:logging -p no:mock -p no:tornado
markers =
    slowmonitoring: (slow) tests of monitoring plugin
    slowstatements: (slow) tests of statements plugin
